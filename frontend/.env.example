# ===== 前端环境变量 =====
# 本地开发：后端运行在 localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_WS_BASE=http://localhost:8000

# 生产部署：把这两行留空（或注释掉），前端走同源，
# 由 nginx 统一反代 /api → 后端、/api/ws → WebSocket。
# NEXT_PUBLIC_API_BASE=
# NEXT_PUBLIC_WS_BASE=
