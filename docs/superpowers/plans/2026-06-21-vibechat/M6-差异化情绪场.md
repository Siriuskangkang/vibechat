# M6 · 差异化情绪场

> **目标**：把"会呼吸的情绪房间"落地——后端 WS 接入 AI 主持破冰（`role=ai`），前端情绪场基建（color 映射 + WS 客户端）+ 三件套组件（房间情绪场 MoodField / 共鸣度 ResonanceBadge / 离场旅程 MoodJourney）。
> **产出**：WS 破冰；`lib/color.ts`、`lib/ws.ts`、`MoodField/ResonanceBadge/MoodJourney.tsx`。
> **前置**：M5 完成（room_mood 推送、mood_end/离场端点已就绪）。

---

## Task 23: 后端 WS 接入 AI 主持破冰

**Files:**
- Modify: `backend/app/ws/rooms_ws.py`

- [ ] **Step 1: rooms_ws.py 顶部补 import**

```python
import asyncio
from ..services.host_service import icebreak, BOT_WELCOME
```
（若 M5 已 import `BOT_WELCOME`，此处改为 `from ..services.host_service import icebreak, BOT_WELCOME`。）

- [ ] **Step 2: 加异步破冰函数**

在 `_broadcast_state` 之后定义：
```python
async def _send_icebreak(slug: str, room, member_emotion: str, recent: list[str]):
    try:
        msg = await icebreak(room.slug, room.vibe, member_emotion, recent)
        await manager.broadcast(slug, {
            "type": "host", "sender": "房间主持", "nickname": "房间主持",
            "role": "ai", "content": msg, "ts": _ts(),
        })
    except Exception:
        pass
```

- [ ] **Step 3: join 分支内触发破冰**

在兜底 bot 逻辑之后追加：
```python
        recent = [m.content for m in message_repo.list_messages(db, room.id)[-5:]]
        member_emotion = "积极" if vector[0] >= 0 else "消极"
        asyncio.create_task(_send_icebreak(slug, room, member_emotion, recent))
```

- [ ] **Step 4: 验证 + Commit**

```bash
cd /Users/kang/Desktop/VibeChat/backend
uvicorn app.main:app --reload --port 8000
python scripts/ws_probe.py
# 期望：除 member_join/presence/room_mood 外，还收到一条 type=host（role=ai）的破冰语（未配 key 走预设话术）
git add backend/app/ws/rooms_ws.py
git commit -m "feat(ws): AI host icebreak on join (role=ai)"
```

---

## Task 24: 前端 color 映射 + WS 客户端

**Files:**
- Create: `frontend/lib/color.ts`
- Create: `frontend/lib/ws.ts`

- [ ] **Step 1: frontend/lib/color.ts**

```typescript
// 情绪向量 [valence, arousal, intensity, social] -> 颜色 / 色盘坐标

export function emotionHsl(vec: number[]): string {
  const [valence, arousal] = vec;
  const hue = valence >= 0 ? 40 : 240;        // 正向暖金 / 负向冷蓝紫
  const light = Math.round(40 + arousal * 40);
  return `hsl(${hue}, 70%, ${light}%)`;
}

// 色盘坐标：x = valence[-1,1] -> [2,98]%，y = arousal[0,1] -> [2,98]%（上高下低）
export function wheelPos(vec: number[]): { x: number; y: number } {
  const [valence, arousal] = vec;
  return { x: 2 + ((valence + 1) / 2) * 96, y: 2 + (1 - arousal) * 96 };
}

export function affinityColor(affinity: number): string {
  // 0-100 -> 冷->暖
  const hue = Math.round(240 - (affinity / 100) * 200);
  return `hsl(${hue}, 70%, 55%)`;
}
```

- [ ] **Step 2: frontend/lib/ws.ts**

```typescript
"use client";
import { useEffect, useRef, useState, useCallback } from "react";
import type { ChatMessage, RoomMood } from "./types";

export function useRoom(slug: string, vector: number[], sessionId: string) {
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [mood, setMood] = useState<RoomMood | null>(null);
  const wsRef = useRef<WebSocket | null>(null);

  useEffect(() => {
    const base = process.env.NEXT_PUBLIC_WS_BASE || (typeof window !== "undefined" ? window.location.origin : "");
    const wsUrl = base.replace(/^http/, "ws") + `/api/ws/room/${slug}`;
    const ws = new WebSocket(wsUrl);
    wsRef.current = ws;
    ws.onopen = () => ws.send(JSON.stringify({ type: "join", session_id: sessionId, vector }));
    ws.onmessage = (e) => {
      const m = JSON.parse(e.data);
      if (m.type === "message" || m.type === "host" || m.type === "history") {
        const role = (m.role || (m.type === "host" ? "ai" : "user")) as ChatMessage["role"];
        setMessages((p) => [...p, { sender: m.sender || m.nickname, nickname: m.nickname, role, content: m.content, ts: m.ts, avatar: m.avatar }]);
      } else if (m.type === "room_mood") {
        setMood({ vector: m.vector, resonance: m.resonance, online: m.online });
      } else if (m.type === "presence") {
        setMood((p) => (p ? { ...p, online: m.online } : { vector: [0, 0, 0, 0], resonance: 0, online: m.online }));
      }
    };
    return () => ws.close();
  }, [slug, sessionId, vector.join(",")]);

  const send = useCallback((content: string) => {
    wsRef.current?.send(JSON.stringify({ type: "message", content }));
  }, []);

  return { messages, mood, send };
}
```

- [ ] **Step 3: 前端本地环境 frontend/.env.local**

```
NEXT_PUBLIC_API_BASE=http://localhost:8000
NEXT_PUBLIC_WS_BASE=http://localhost:8000
```

- [ ] **Step 4: Commit**

```bash
cd /Users/kang/Desktop/VibeChat
git add frontend/lib/color.ts frontend/lib/ws.ts frontend/.env.local
git commit -m "feat(frontend): color mapping + websocket room hook"
```

---

## Task 25: 情绪场三件套组件

**Files:**
- Create: `frontend/components/MoodField.tsx`
- Create: `frontend/components/ResonanceBadge.tsx`
- Create: `frontend/components/MoodJourney.tsx`

- [ ] **Step 1: frontend/components/MoodField.tsx**（房间集体情绪色场）

```tsx
"use client";
import { emotionHsl } from "@/lib/color";

export function MoodField({ vector, children }: { vector: number[]; children?: React.ReactNode }) {
  const color = emotionHsl(vector);
  return (
    <div className="absolute inset-0 -z-10 transition-all duration-[2000ms]"
         style={{ background: `radial-gradient(circle at 50% 40%, ${color} 0%, rgba(14,15,26,0.9) 70%)` }}>
      <div className="absolute inset-0 opacity-30 animate-pulse" style={{ background: color, filter: "blur(60px)" }} />
      {children}
    </div>
  );
}
```

- [ ] **Step 2: frontend/components/ResonanceBadge.tsx**

```tsx
"use client";
export function ResonanceBadge({ resonance, online }: { resonance: number; online: number }) {
  const pct = Math.round(resonance * 100);
  return (
    <div className="flex items-center gap-3 text-sm text-slate-300">
      <span className="px-2 py-1 rounded-full bg-white/10">此刻共鸣度 {pct}%</span>
      <span className="px-2 py-1 rounded-full bg-white/10">{online} 人在线</span>
    </div>
  );
}
```

- [ ] **Step 3: frontend/components/MoodJourney.tsx**（离场情绪旅程：start→mood_end 轨迹）

```tsx
"use client";
import { wheelPos, emotionHsl } from "@/lib/color";

export function MoodJourney({
  startVector,
  moodEnd,
}: {
  startVector: number[];
  moodEnd: { valence: number; arousal: number };
}) {
  const a = wheelPos(startVector);
  const b = wheelPos([moodEnd.valence, moodEnd.arousal, 0.5, 0.5]);
  return (
    <div className="w-full max-w-md aspect-square relative mx-auto">
      <svg viewBox="0 0 100 100" className="w-full h-full">
        <rect x="0" y="0" width="100" height="100" rx="8" fill="#15162a" />
        <line x1="50" y1="0" x2="50" y2="100" stroke="#ffffff15" />
        <line x1="0" y1="50" x2="100" y2="50" stroke="#ffffff15" />
        <path d={`M ${a.x} ${a.y} Q 50 50 ${b.x} ${b.y}`} fill="none" stroke="#ffffff60" strokeWidth="0.8" strokeDasharray="2 2" />
        <circle cx={a.x} cy={a.y} r="2.5" fill={emotionHsl(startVector)} />
        <circle cx={b.x} cy={b.y} r="2.5" fill={emotionHsl([moodEnd.valence, moodEnd.arousal, 0.5, 0.5])} />
      </svg>
      <p className="text-center text-sm text-slate-400 mt-2">你来时 · 此刻</p>
    </div>
  );
}
```

- [ ] **Step 4: 验证编译 + Commit**

```bash
cd /Users/kang/Desktop/VibeChat/frontend
npm run build   # 期望编译通过（组件未被页面引用时 Next 可能警告，忽略）
git add frontend/components/MoodField.tsx frontend/components/ResonanceBadge.tsx frontend/components/MoodJourney.tsx
git commit -m "feat(frontend): mood field / resonance badge / mood journey components"
```

---

## M6 验收

- [ ] WS join 后收到 `type=host`（role=ai）破冰语
- [ ] `color.ts` / `ws.ts` 编译通过；`useRoom` 能连 WS 并更新 messages/mood
- [ ] 三件套组件编译通过（M7 在房间页/离场页接线）

**满足后进入 [M7 前端](./M7-前端.md)。**
