# M2 · 双接口适配层（TDD）

> **目标**：实现 LLM 统一抽象 + OpenAI/Anthropic 双实现 + 工厂切换。**这是技术性 40% 的核心 + 严重扣分项②的防御**。用 respx mock 拦截 httpx，不调真 API 验证"请求体构造正确 + 响应解析正确"。
> **产出**：`llm/{base,openai_client,anthropic_client,factory}.py` + 单测全绿。
> **前置**：M1 完成。

---

## Task 9: 统一抽象 base.py

**Files:**
- Create: `backend/app/llm/__init__.py`（空）
- Create: `backend/app/llm/base.py`

- [ ] **Step 1: 写 backend/app/llm/base.py**

```python
from dataclasses import dataclass
from typing import Optional, Protocol


@dataclass
class LLMResponse:
    content: str
    raw_json: Optional[dict]
    usage: dict
    provider: str
    model: str


class LLMClient(Protocol):
    provider: str

    async def chat(
        self,
        system: str,
        user: str,
        *,
        json_schema: Optional[dict] = None,
        temperature: float = 0.3,
        max_tokens: int = 1024,
    ) -> LLMResponse: ...
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/llm
git commit -m "feat(llm): LLMClient protocol and LLMResponse"
```

---

## Task 10: OpenAI 标准实现 OpenAIClient

**Files:**
- Create: `backend/app/llm/openai_client.py`
- Create: `backend/tests/test_llm_openai.py`

- [ ] **Step 1: 写失败测试 backend/tests/test_llm_openai.py**

```python
import json
import httpx
import respx
from app.llm.openai_client import OpenAIClient


@respx.mock
async def test_openai_structured_request_and_parse():
    respx.post("https://ex.com/v1/chat/completions").mock(
        return_value=httpx.Response(200, json={
            "choices": [{"message": {"content": '{"primary":"焦虑","valence":-0.7}'}}],
            "usage": {"prompt_tokens": 10, "completion_tokens": 5},
        })
    )
    client = OpenAIClient("https://ex.com/v1", "sk-x", "qwen-plus")
    resp = await client.chat("你是情绪分析", "睡不着", json_schema={"type": "object"})

    # 响应解析正确
    assert resp.provider == "openai"
    assert resp.model == "qwen-plus"
    assert resp.raw_json == {"primary": "焦虑", "valence": -0.7}
    assert resp.usage == {"input_tokens": 10, "output_tokens": 5}

    # 请求体构造正确（OpenAI 标准：response_format + system 在 messages）
    body = json.loads(respx.calls[0].request.content)
    assert body["model"] == "qwen-plus"
    assert body["response_format"] == {"type": "json_object"}
    assert body["messages"][0] == {"role": "system", "content": "你是情绪分析"}
    assert body["messages"][1] == {"role": "user", "content": "睡不着"}
    # 鉴权头
    assert respx.calls[0].request.headers["authorization"] == "Bearer sk-x"


@respx.mock
async def test_openai_plain_text():
    respx.post("https://ex.com/v1/chat/completions").mock(
        return_value=httpx.Response(200, json={
            "choices": [{"message": {"content": "你好"}}],
            "usage": {"prompt_tokens": 1, "completion_tokens": 1},
        })
    )
    client = OpenAIClient("https://ex.com/v1", "sk-x", "qwen-plus")
    resp = await client.chat("s", "u")
    assert resp.raw_json is None
    assert resp.content == "你好"
```

- [ ] **Step 2: 跑测试见失败**

```bash
cd /Users/kang/Desktop/VibeChat/backend
pytest tests/test_llm_openai.py -v
```
Expected: FAIL（`ModuleNotFoundError: app.llm.openai_client`）

- [ ] **Step 3: 写实现 backend/app/llm/openai_client.py**

```python
import json
import httpx
from .base import LLMResponse


class OpenAIClient:
    """OpenAI 标准接口实现（/chat/completions）。"""

    provider = "openai"

    def __init__(self, base_url: str, api_key: str, model: str):
        self.base_url = base_url
        self.api_key = api_key
        self.model = model

    async def chat(self, system, user, *, json_schema=None, temperature=0.3, max_tokens=1024) -> LLMResponse:
        headers = {"Authorization": f"Bearer {self.api_key}", "Content-Type": "application/json"}
        body = {
            "model": self.model,
            "messages": [
                {"role": "system", "content": system},
                {"role": "user", "content": user},
            ],
            "temperature": temperature,
            "max_tokens": max_tokens,
        }
        if json_schema:
            body["response_format"] = {"type": "json_object"}

        async with httpx.AsyncClient(timeout=60) as c:
            r = await c.post(f"{self.base_url}/chat/completions", headers=headers, json=body)
            r.raise_for_status()
            data = r.json()

        text = data["choices"][0]["message"]["content"]
        raw = json.loads(text) if json_schema else None
        usage = {
            "input_tokens": data["usage"]["prompt_tokens"],
            "output_tokens": data["usage"]["completion_tokens"],
        }
        return LLMResponse(content=text, raw_json=raw, usage=usage, provider="openai", model=self.model)
```

- [ ] **Step 4: 跑测试通过**

```bash
pytest tests/test_llm_openai.py -v
```
Expected: `2 passed`

- [ ] **Step 5: Commit**

```bash
git add backend/app/llm/openai_client.py backend/tests/test_llm_openai.py
git commit -m "feat(llm): OpenAI-standard client with request/parse tests"
```

---

## Task 11: Anthropic 标准实现 AnthropicClient

**Files:**
- Create: `backend/app/llm/anthropic_client.py`
- Create: `backend/tests/test_llm_anthropic.py`

- [ ] **Step 1: 写失败测试 backend/tests/test_llm_anthropic.py**

```python
import json
import httpx
import respx
from app.llm.anthropic_client import AnthropicClient


@respx.mock
async def test_anthropic_structured_system_separate_and_prefill():
    # 模型续写：我们 prefill 了 "{"，模型返回剩余
    respx.post("https://ex.com/v1/messages").mock(
        return_value=httpx.Response(200, json={
            "content": [{"type": "text", "text": '"primary":"焦虑","valence":-0.7}'}],
            "usage": {"input_tokens": 10, "output_tokens": 5},
        })
    )
    client = AnthropicClient("https://ex.com/v1", "sk-x", "deepseek-chat")
    resp = await client.chat("你是情绪分析", "睡不着", json_schema={"type": "object"})

    # 解析正确（client 补回 prefill 的 "{"）
    assert resp.provider == "anthropic"
    assert resp.raw_json == {"primary": "焦虑", "valence": -0.7}
    assert resp.usage == {"input_tokens": 10, "output_tokens": 5}

    # 请求体构造正确（Anthropic 标准：system 单独 + prefill assistant "{"）
    body = json.loads(respx.calls[0].request.content)
    assert body["model"] == "deepseek-chat"
    assert body["system"] == "你是情绪分析"          # ★ system 单独字段
    assert body["messages"][0] == {"role": "user", "content": "睡不着"}
    assert body["messages"][-1] == {"role": "assistant", "content": "{"}  # ★ prefill
    # 鉴权头
    assert respx.calls[0].request.headers["x-api-key"] == "sk-x"
    assert respx.calls[0].request.headers["anthropic-version"] == "2023-06-01"


@respx.mock
async def test_anthropic_plain_text():
    respx.post("https://ex.com/v1/messages").mock(
        return_value=httpx.Response(200, json={
            "content": [{"type": "text", "text": "你好"}],
            "usage": {"input_tokens": 1, "output_tokens": 1},
        })
    )
    client = AnthropicClient("https://ex.com/v1", "sk-x", "deepseek-chat")
    resp = await client.chat("s", "u")
    assert resp.raw_json is None
    assert resp.content == "你好"
```

- [ ] **Step 2: 跑测试见失败**

- [ ] **Step 3: 写实现 backend/app/llm/anthropic_client.py**

```python
import json
import re
import httpx
from .base import LLMResponse


def _parse_json_lenient(text: str):
    """宽容解析：提取首个 {...} 平衡块再 json.loads。"""
    m = re.search(r"\{.*\}", text, re.S)
    if not m:
        return None
    try:
        return json.loads(m.group(0))
    except json.JSONDecodeError:
        return None


class AnthropicClient:
    """Anthropic 标准接口实现（/v1/messages）。"""

    provider = "anthropic"

    def __init__(self, base_url: str, api_key: str, model: str):
        self.base_url = base_url
        self.api_key = api_key
        self.model = model

    async def chat(self, system, user, *, json_schema=None, temperature=0.3, max_tokens=1024) -> LLMResponse:
        headers = {
            "x-api-key": self.api_key,
            "anthropic-version": "2023-06-01",
            "Content-Type": "application/json",
        }
        messages = [{"role": "user", "content": user}]
        if json_schema:
            messages.append({"role": "assistant", "content": "{"})  # prefill 引导 JSON

        body = {
            "model": self.model,
            "system": system,            # ★ system 单独传
            "messages": messages,
            "max_tokens": max_tokens,
            "temperature": temperature,
        }

        async with httpx.AsyncClient(timeout=60) as c:
            r = await c.post(f"{self.base_url}/v1/messages", headers=headers, json=body)
            r.raise_for_status()
            data = r.json()

        text = "".join(b.get("text", "") for b in data["content"] if b.get("type") == "text")
        if json_schema:
            text = "{" + text  # 补回 prefill
        raw = _parse_json_lenient(text) if json_schema else None
        usage = {
            "input_tokens": data["usage"]["input_tokens"],
            "output_tokens": data["usage"]["output_tokens"],
        }
        return LLMResponse(content=text, raw_json=raw, usage=usage, provider="anthropic", model=self.model)
```

- [ ] **Step 4: 跑测试通过**

```bash
pytest tests/test_llm_anthropic.py -v
```
Expected: `2 passed`

- [ ] **Step 5: Commit**

```bash
git add backend/app/llm/anthropic_client.py backend/tests/test_llm_anthropic.py
git commit -m "feat(llm): Anthropic-standard client (system/prefill) with tests"
```

---

## Task 12: 工厂 factory.py

**Files:**
- Create: `backend/app/llm/factory.py`
- Create: `backend/tests/test_llm_factory.py`

- [ ] **Step 1: 写失败测试 backend/tests/test_llm_factory.py**

```python
from app.core.config import settings
from app.llm.factory import get_llm
from app.llm.openai_client import OpenAIClient
from app.llm.anthropic_client import AnthropicClient


def test_factory_returns_openai(monkeypatch):
    monkeypatch.setattr(settings, "llm_provider", "openai")
    assert isinstance(get_llm(), OpenAIClient)


def test_factory_returns_anthropic(monkeypatch):
    monkeypatch.setattr(settings, "llm_provider", "anthropic")
    assert isinstance(get_llm(), AnthropicClient)
```

- [ ] **Step 2: 跑测试见失败**

- [ ] **Step 3: 写实现 backend/app/llm/factory.py**

```python
from ..core.config import settings
from .anthropic_client import AnthropicClient
from .base import LLMClient
from .openai_client import OpenAIClient


def get_llm() -> LLMClient:
    """读 LLM_PROVIDER 返回对应实现，上层零感知。"""
    if settings.llm_provider == "anthropic":
        return AnthropicClient(
            settings.anthropic_base_url,
            settings.anthropic_api_key,
            settings.anthropic_model,
        )
    return OpenAIClient(
        settings.openai_base_url,
        settings.openai_api_key,
        settings.openai_model,
    )
```

- [ ] **Step 4: 跑测试通过**

```bash
pytest tests/test_llm_factory.py -v
```
Expected: `2 passed`

- [ ] **Step 5: 全量回归 + Commit**

```bash
pytest tests/ -v   # 全绿
git add backend/app/llm/factory.py backend/tests/test_llm_factory.py
git commit -m "feat(llm): provider factory with switch tests"
```

---

## M2 验收

- [ ] `pytest backend/tests/ -v` 全绿
- [ ] 两个 client 的"请求体构造 + 响应解析"都被单测锁定（OpenAI 的 `response_format`+system-in-messages；Anthropic 的 `system` 单独 + prefill）
- [ ] `get_llm()` 随 `LLM_PROVIDER` 切换返回不同实现

**满足后进入 [M3 情绪分析与匹配](./M3-情绪分析与匹配.md)。**
