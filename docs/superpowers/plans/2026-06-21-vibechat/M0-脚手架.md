# M0 · 脚手架

> **本里程碑目标**：搭起前后端骨架，配置走通，DB 建表 + 种子房间，health 可访问，前端能跑。
> **产出**：可启动的 FastAPI（`/api/health` 返回 provider）+ 可启动的 Next.js 首页。
> **前置**：`cd /Users/kang/Desktop/VibeChat`，Python 3.11+ / Node 18+ 已装。

---

## Task 1: git init + 后端骨架 + config

**Files:**
- Create: `.gitignore`
- Create: `backend/requirements.txt`
- Create: `backend/.env.example`
- Create: `backend/app/__init__.py`（空）
- Create: `backend/app/core/__init__.py`（空）
- Create: `backend/app/core/config.py`
- Create: `backend/app/core/exceptions.py`

- [ ] **Step 1: git init + .gitignore**

```bash
cd /Users/kang/Desktop/VibeChat
git init
```

`.gitignore`：
```
__pycache__/
*.pyc
.venv/
backend/.env
*.db
node_modules/
.next/
.DS_Store
```

- [ ] **Step 2: backend/requirements.txt**

```
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2
pydantic-settings>=2
httpx>=0.27
pytest>=8
pytest-asyncio>=0.23
respx>=0.21
```

- [ ] **Step 3: backend/.env.example**（占位，真实值在 .env）

```bash
LLM_PROVIDER=openai
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_API_KEY=sk-***
OPENAI_MODEL=qwen-plus
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_API_KEY=sk-***
ANTHROPIC_MODEL=deepseek-chat
DATABASE_URL=sqlite:///./vibechat.db
LOG_LEVEL=INFO
```

- [ ] **Step 4: backend/app/core/config.py**

```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")

    llm_provider: str = "openai"
    openai_base_url: str = "https://dashscope.aliyuncs.com/compatible-mode/v1"
    openai_api_key: str = ""
    openai_model: str = "qwen-plus"
    anthropic_base_url: str = "https://api.deepseek.com/anthropic"
    anthropic_api_key: str = ""
    anthropic_model: str = "deepseek-chat"
    database_url: str = "sqlite:///./vibechat.db"
    log_level: str = "INFO"
    daily_analysis_limit_guest: int = 10
    message_rate_limit_per_min: int = 30


settings = Settings()
```

- [ ] **Step 5: backend/app/core/exceptions.py**

```python
from fastapi import Request
from fastapi.responses import JSONResponse


class VibeChatError(Exception):
    def __init__(self, code: str, message: str, status: int = 400):
        self.code = code
        self.message = message
        self.status = status


def register_exception_handlers(app):
    @app.exception_handler(VibeChatError)
    async def _handle(req: Request, exc: VibeChatError):
        return JSONResponse(
            status_code=exc.status,
            content={"code": exc.code, "message": exc.message, "data": None},
        )
```

- [ ] **Step 6: 建空 `__init__.py`**（`backend/app/__init__.py`、`backend/app/core/__init__.py`），内容为空。

- [ ] **Step 7: 验证 config 可加载**

```bash
cd /Users/kang/Desktop/VibeChat/backend
python -m venv .venv && source .venv/bin/activate
pip install pydantic-settings
cp .env.example .env   # 临时，验证后保留（已 gitignore）
python -c "from app.core.config import settings; print(settings.llm_provider, settings.database_url)"
```
Expected: `openai sqlite:///./vibechat.db`

- [ ] **Step 8: Commit**

```bash
cd /Users/kang/Desktop/VibeChat
git add .gitignore backend/requirements.txt backend/.env.example backend/app
git commit -m "chore: backend scaffold, config, exceptions"
```

---

## Task 2: DB 模型 + database.py + seed 房间

**Files:**
- Create: `backend/app/models.py`
- Create: `backend/app/database.py`
- Create: `backend/app/seeds/rooms.seed.json`
- Create: `backend/tests/__init__.py`（空）
- Create: `backend/tests/test_db_seed.py`

- [ ] **Step 1: backend/app/models.py**（5 表；`RoomMembership.vector` 为进房情绪向量快照，供情绪场聚合）

```python
import uuid
from datetime import datetime, timezone
from sqlalchemy import String, Float, Text, Boolean, DateTime, ForeignKey, JSON
from sqlalchemy.orm import Mapped, mapped_column, DeclarativeBase


def _uuid() -> str:
    return str(uuid.uuid4())


def _now() -> datetime:
    return datetime.now(timezone.utc)


class Base(DeclarativeBase):
    pass


class Session(Base):
    __tablename__ = "sessions"
    id: Mapped[str] = mapped_column(String, primary_key=True, default=_uuid)
    nickname: Mapped[str | None] = mapped_column(String(64), nullable=True)
    avatar: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=_now)
    last_seen_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=_now)


class EmotionAnalysis(Base):
    __tablename__ = "emotion_analyses"
    id: Mapped[str] = mapped_column(String, primary_key=True, default=_uuid)
    session_id: Mapped[str] = mapped_column(ForeignKey("sessions.id"), index=True)
    input_text: Mapped[str] = mapped_column(Text)
    primary: Mapped[str] = mapped_column(String(32))
    secondary: Mapped[list] = mapped_column(JSON, default=list)
    valence: Mapped[float] = mapped_column(Float)
    arousal: Mapped[float] = mapped_column(Float)
    intensity: Mapped[float] = mapped_column(Float)
    social: Mapped[float] = mapped_column(Float)
    vector: Mapped[list] = mapped_column(JSON)
    keywords: Mapped[list] = mapped_column(JSON, default=list)
    reading: Mapped[str] = mapped_column(Text)
    risk_flag: Mapped[bool] = mapped_column(Boolean, default=False)
    provider: Mapped[str] = mapped_column(String(16), default="openai")
    model: Mapped[str] = mapped_column(String(64), default="")
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=_now, index=True)


class Room(Base):
    __tablename__ = "rooms"
    id: Mapped[str] = mapped_column(String, primary_key=True, default=_uuid)
    slug: Mapped[str] = mapped_column(String(48), unique=True, index=True)
    name: Mapped[str] = mapped_column(String(48))
    description: Mapped[str] = mapped_column(Text)
    anchor_vector: Mapped[list] = mapped_column(JSON)
    color: Mapped[str] = mapped_column(String(16))
    vibe: Mapped[str] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=_now)


class RoomMembership(Base):
    __tablename__ = "room_memberships"
    id: Mapped[str] = mapped_column(String, primary_key=True, default=_uuid)
    room_id: Mapped[str] = mapped_column(ForeignKey("rooms.id"), index=True)
    session_id: Mapped[str] = mapped_column(ForeignKey("sessions.id"), index=True)
    nickname: Mapped[str] = mapped_column(String(64))
    avatar: Mapped[dict | None] = mapped_column(JSON, nullable=True)
    vector: Mapped[list] = mapped_column(JSON)  # 进房情绪向量快照（情绪场聚合用）
    joined_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=_now)
    left_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)


class Message(Base):
    __tablename__ = "messages"
    id: Mapped[str] = mapped_column(String, primary_key=True, default=_uuid)
    room_id: Mapped[str] = mapped_column(ForeignKey("rooms.id"), index=True)
    session_id: Mapped[str | None] = mapped_column(ForeignKey("sessions.id"), nullable=True)
    sender_role: Mapped[str] = mapped_column(String(8))  # user | ai | bot
    nickname: Mapped[str] = mapped_column(String(64))
    content: Mapped[str] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=_now, index=True)
```

- [ ] **Step 2: backend/app/seeds/rooms.seed.json**（6 间房，anchor 见设计 03 §5）

```json
[
  {"slug":"late-night-anxiety","name":"深夜焦虑","description":"被明天的某件事压着，脑子停不下来，睡不着","anchor_vector":[-0.6,0.80,0.80,0.60],"color":"#6D5BFF","vibe":"紧绷、低频嗡鸣、深夜"},
  {"slug":"emo","name":"emo 时刻","description":"有点闷，说不上来，情绪低落","anchor_vector":[-0.50,0.30,0.60,0.55],"color":"#7C8DB5","vibe":"低沉、缓慢、留白"},
  {"slug":"joyful-share","name":"开心想分享","description":"有开心事，想抖出来，希望有人接住这份高兴","anchor_vector":[0.70,0.60,0.60,0.75],"color":"#FFB26B","vibe":"明亮、上扬、暖"},
  {"slug":"vent-anger","name":"愤怒吐槽","description":"被什么气到了，想先骂为敬","anchor_vector":[-0.60,0.85,0.85,0.70],"color":"#FF6B5E","vibe":"炽烈、张力、快"},
  {"slug":"calm-solo","name":"平静独处","description":"今天挺安静的，想随便待会儿","anchor_vector":[0.30,0.15,0.25,0.20],"color":"#5BCFA8","vibe":"柔和、留白、慢"},
  {"slug":"lost-advice","name":"迷茫求建议","description":"卡住了，想说说正纠结的事，也许能捋一捋","anchor_vector":[-0.20,0.40,0.50,0.80],"color":"#9B7BE0","vibe":"思忖、低张力、求帮"}
]
```

- [ ] **Step 3: backend/app/database.py**

```python
import json
from pathlib import Path
from sqlalchemy import create_engine, select
from sqlalchemy.orm import sessionmaker
from .core.config import settings
from .models import Base, Room

_connect = {"check_same_thread": False} if settings.database_url.startswith("sqlite") else {}
engine = create_engine(settings.database_url, connect_args=_connect)
SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def init_db():
    Base.metadata.create_all(bind=engine)
    seed_rooms()


def seed_rooms():
    db = SessionLocal()
    try:
        if db.scalar(select(Room).limit(1)):
            return
        seed_path = Path(__file__).parent / "seeds" / "rooms.seed.json"
        for r in json.loads(seed_path.read_text(encoding="utf-8")):
            db.add(Room(**r))
        db.commit()
    finally:
        db.close()
```

- [ ] **Step 4: 写测试 backend/tests/test_db_seed.py**

```python
from sqlalchemy import create_engine, select
from app.database import init_db
from app.models import Room


def test_init_db_seeds_six_rooms(tmp_path, monkeypatch):
    monkeypatch.setenv("DATABASE_URL", f"sqlite:///{tmp_path}/t.db")
    # 重新导入以应用新的 db url
    import importlib
    import app.database as dbmod
    importlib.reload(dbmod)
    dbmod.init_db()
    from sqlalchemy.orm import sessionmaker
    s = sessionmaker(bind=dbmod.engine)()
    rooms = s.scalars(select(Room)).all()
    assert len(rooms) == 6
    assert {r.slug for r in rooms} == {
        "late-night-anxiety", "emo", "joyful-share",
        "vent-anger", "calm-solo", "lost-advice",
    }
```

- [ ] **Step 5: 安装依赖并跑测试**

```bash
cd /Users/kang/Desktop/VibeChat/backend
pip install -r requirements.txt
pip install pytest
pytest tests/test_db_seed.py -v
```
Expected: `1 passed`

- [ ] **Step 6: Commit**

```bash
git add backend/app/models.py backend/app/database.py backend/app/seeds backend/tests
git commit -m "feat(db): models, init_db, 6 room seeds with tests"
```

---

## Task 3: FastAPI main + health 路由

**Files:**
- Create: `backend/app/routers/__init__.py`（空）
- Create: `backend/app/routers/health.py`
- Create: `backend/app/main.py`

- [ ] **Step 1: backend/app/routers/health.py**

```python
from fastapi import APIRouter
from ..core.config import settings

router = APIRouter()


@router.get("/health")
async def health():
    return {"code": 0, "message": "ok", "data": {"status": "up", "provider": settings.llm_provider}}
```

- [ ] **Step 2: backend/app/main.py**

```python
from contextlib import asynccontextmanager
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from .database import init_db
from .core.exceptions import register_exception_handlers
from .routers import health


@asynccontextmanager
async def lifespan(app: FastAPI):
    init_db()
    yield


app = FastAPI(title="VibeChat", lifespan=lifespan)
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)
register_exception_handlers(app)
app.include_router(health.router, prefix="/api")
```

- [ ] **Step 3: 验证启动**

```bash
cd /Users/kang/Desktop/VibeChat/backend
uvicorn app.main:app --reload --port 8000
# 另开终端：
curl -s http://127.0.0.1:8000/api/health
```
Expected: `{"code":0,"message":"ok","data":{"status":"up","provider":"openai"}}`

- [ ] **Step 4: Commit**

```bash
git add backend/app/main.py backend/app/routers
git commit -m "feat(api): fastapi app, health endpoint, cors, exception handlers"
```

---

## Task 4: 前端 Next.js 骨架 + types + api 封装

**Files:**
- Create: `frontend/`（create-next-app 生成）
- Create: `frontend/lib/types.ts`
- Create: `frontend/lib/api.ts`
- Modify: `frontend/app/page.tsx`（占位首页）

- [ ] **Step 1: 创建 Next.js 项目**

```bash
cd /Users/kang/Desktop/VibeChat
npx create-next-app@latest frontend --typescript --tailwind --app --no-src-dir --no-eslint --use-npm --import-alias "@/*"
```
（交互项均按上述 flag 自动选择；若提示确认，回车默认。）

- [ ] **Step 2: frontend/lib/types.ts**

```typescript
export interface EmotionResult {
  primary: string;
  secondary: string[];
  valence: number; arousal: number; intensity: number; social: number;
  vector: number[];
  keywords: string[];
  reading: string;
  risk_flag: boolean;
  provider: string;
}
export interface RoomBrief { slug: string; name: string; color: string; description: string; }
export interface MatchResult {
  emotion: EmotionResult;
  room: RoomBrief;
  affinity: number;
  alternatives: { slug: string; name: string; color: string; affinity: number }[];
}
export interface Avatar { color: string; shape: string; }
export interface ChatMessage {
  sender: string;
  nickname: string;
  role: "user" | "ai" | "bot";
  content: string;
  ts: number;
  avatar?: Avatar;
}
export interface RoomMood { vector: number[]; resonance: number; online: number; }
```

- [ ] **Step 3: frontend/lib/api.ts**

```typescript
const BASE = process.env.NEXT_PUBLIC_API_BASE || "";

export async function postJSON<T>(path: string, body: unknown): Promise<T> {
  const r = await fetch(`${BASE}${path}`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  });
  const j = await r.json();
  if (!r.ok || j.code !== 0) throw new Error(j.message || "request failed");
  return j.data as T;
}

export async function getJSON<T>(path: string): Promise<T> {
  const r = await fetch(`${BASE}${path}`);
  const j = await r.json();
  if (!r.ok || j.code !== 0) throw new Error(j.message || "request failed");
  return j.data as T;
}
```

- [ ] **Step 4: frontend/app/page.tsx**（占位，M7 再实装）

```tsx
export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-[#0E0F1A] text-slate-200">
      <h1 className="text-2xl">VibeChat · 情绪社交（脚手架）</h1>
    </main>
  );
}
```

- [ ] **Step 5: 验证启动**

```bash
cd /Users/kang/Desktop/VibeChat/frontend
npm run dev
# 浏览器开 http://localhost:3000 看到"VibeChat · 情绪社交（脚手架）"
```

- [ ] **Step 6: Commit**

```bash
cd /Users/kang/Desktop/VibeChat
git add frontend
git commit -m "feat(frontend): next.js scaffold, types, api client"
```

---

## M0 验收

- [ ] `curl http://127.0.0.1:8000/api/health` 返回 provider
- [ ] `pytest backend/tests/test_db_seed.py` 通过，6 间房入库
- [ ] `http://localhost:3000` 显示占位首页
- [ ] `.env` 未被提交（`git status` 干净）

**满足后进入 [M1 核心纯逻辑](./M1-核心纯逻辑.md)。**
